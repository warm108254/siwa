# Quantity Module (`src/modules/quantity`)

Purpose: calculate quantities from BOQ entities and dimensions.

Planned responsibilities:
- Compute lengths/areas/volumes/counts.
- Apply unit conversion and precision policies.
- Expose traceable calculation steps.

Notes:
- All calculations should preserve provenance (which page/entity/rule produced the value).
