# Classifier Module (`src/modules/classifier`)

Purpose: classify drawing pages before detailed parsing.

Planned responsibilities:
- Identify page type (architectural/structural/MEP/detail/schedule).
- Route page to proper parsing templates.
- Attach confidence scores for verification.

Notes:
- Classification output should be explicit and auditable for QA.
