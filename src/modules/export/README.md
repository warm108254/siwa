# Export Module (`src/modules/export`)

Purpose: generate deliverables from validated BOQ data.

Planned responsibilities:
- Export Excel outputs for procurement workflows.
- Export PDF summary reports.
- Preserve job/version metadata in exported files.

Notes:
- Exports should remain deterministic for auditability.
