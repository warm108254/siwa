# 15s Product Presentation Video

This repository contains a helper script to generate a 15-second presentation video from a single image:

```bash
./create_promo_video.sh input.jpg output.Dotmp4
```

The output extension is `.Dotmp4` as requested.
